spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/qas
    username: postgres
    password: kosua2468
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:change-this-development-secret-key-to-at-least-32-characters}
  expiration: ${JWT_EXPIRATION:86400000}
