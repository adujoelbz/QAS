spring:
  jpa:
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5433/qas
    username: postgres
    password: kosua2468
  flyway:
    enabled: true
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: aduj30628@gmail.com
    password: eozk udie aibs ryf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


jwt:
  secret: change-this-development-secret-key-to-at-least-32-characters
  expiration: 86400000
cloudinary:
  cloud-name: w6ifrhsl
  api-key: 117475164841572
  api-secret: CEwh9YkvQ1Kc3mLNxUUVPzeIIWo
  secure: true

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173
  notification:
      email-from: noreply@queueless.com   # Hardcoded for development
      base-url: ${APP_BASE_URL:http://localhost:8080}

ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:5000}
    timeout: 5000  # milliseconds
    enabled: ${AI_ENABLED:true}

logging:
  pattern:
    level: "%5p [corr=%X{correlationId:-none}]"
